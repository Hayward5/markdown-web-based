# 專案交接文件 - Markdown 編輯器

> **目的**：將此文件提供給新環境的 AI，使其能理解專案上下文並繼續開發。

---

## 專案概述

開發一個 **離線可用的 WYSIWYG Markdown 編輯器**，部署於公司內網（無網際網路）。

### 技術方案
- **框架**：Milkdown/Crepe（基於 ProseMirror）
- **打包**：Vite
- **最終產出**：純 HTML/JS/CSS 靜態檔案

### 參考專案
專案內有 `vscode-main/` 資料夾，這是 Milkdown 的 VSCode 擴充套件原始碼，作為實作參考。

---

## 已完成的工作

### ✅ 近期修正
- 表格最小列數限制：GFM table schema 含 `table_header_row`，表頭列不可刪除，因此 row 無法小於「header + 1 body」，導致無法縮到 1 row

### ✅ 近期修正
- 開啟檔案錯誤修正（移除 setMarkdown，改用 replaceAll）
- 預覽模式開檔同步更新
- 預覽 task list marker 移除（含巢狀）
- Backspace 退出清單行為：移出段落並重建清單上下區塊
- 無序清單第 3/4 層改為三角 marker，並統一大小（編輯/預覽一致）
- Vite 改為多檔輸出，新增 `build:singlefile` 產出 `dist/testing_index.html`


### ✅ 專案結構（已建立）
```
markdown-web-based/
├── index.html              ✅ 主頁面
├── package.json            ✅ npm 設定
├── vite.config.js          ✅ Vite 打包設定
├── src/
│   ├── main.js             ✅ 入口點
│   ├── editor-manager.js   ✅ 編輯器管理（參考 vscode-main）
│   ├── file-handler.js     ✅ 檔案操作模組
│   └── style/
│       ├── token.css       ✅ CSS 變數（護眼主題）
│       ├── theme.css       ✅ Milkdown 樣式
│       └── main.css        ✅ 應用程式樣式
├── lib/
│   ├── marked.min.js       ✅ 已下載（備用）
│   └── html2pdf.bundle.min.js  ✅ 已移除 PDF 功能
└── vscode-main/            📁 參考專案
```

### ✅ 功能規格（已確認）
- WYSIWYG 編輯（無分割視窗）
- 純預覽模式
- Slash 命令 (`/h1`, `/ul`, `/code` 等)
- 清單自動行為（Enter 續行、Tab 縮排）
- 清單退出行為（Backspace 移除 marker 後移出清單）
- 檔案操作（開啟/儲存 TXT）
- 護眼 Arctic Frost 主題
- 繁中 + 英文支援
- 僅支援 Google Chrome


---

## 未完成的工作

### ⏳ 待執行
1. `npm install` - 安裝相依套件
2. `npm run dev` - 開發模式測試
3. 修正可能的錯誤
4. `npm run build` - 打包多檔版本
5. `npm run build:singlefile` - 產生 `dist/testing_index.html`（本地雙擊測試）
6. 測試 `dist/` 與 `testing_index.html` 離線功能


---

## Ubuntu 環境設置

```bash
# 1. 安裝 Node.js
sudo apt update
sudo apt install nodejs npm

# 2. 進入專案目錄
cd /path/to/markdown-web-based

# 3. 安裝相依套件
npm install

# 4. 開發模式
npm run dev

# 5. 打包
npm run build
```

---

## 給 AI 的指示

請執行以下步驟繼續開發：

1. 執行 `npm install` 安裝套件
2. 執行 `npm run dev` 測試開發模式
3. 如有錯誤，根據錯誤訊息修正程式碼
4. 確認功能正常後，執行 `npm run build` 產生多檔版本
5. 執行 `npm run build:singlefile` 產生 `dist/testing_index.html`
6. 測試 `dist/` 與 `testing_index.html` 是否可離線運作
7. 建立 walkthrough.md 記錄驗證結果


---

## 相關文件
- `implementation_plan.md` - 詳細實作計畫
- `task.md` - 任務清單
