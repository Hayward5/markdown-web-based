# Third-party notices

The production bundle includes these direct dependencies:

- `@milkdown/core` 7.21.3 — MIT
- `@milkdown/crepe` 7.21.3 — MIT
- `dompurify` 3.4.12 — Apache-2.0 OR MPL-2.0
- `marked` 18.0.6 — MIT

See the corresponding packages in `package-lock.json` and `node_modules` for full license texts and transitive dependency notices.
